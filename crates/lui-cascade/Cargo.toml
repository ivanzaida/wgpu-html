[package]
name = "lui-cascade"
version = "0.1.0"
edition = "2024"

[dependencies]
lui-html-parser = { path = "../lui-html-parser" }
lui-css-parser = { path = "../lui-css-parser" }
bumpalo = { version = "3", features = ["collections"] }
