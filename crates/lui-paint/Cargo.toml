[package]
name = "lui-paint"
version = "0.1.0"
edition = "2024"

[dependencies]
lui-core = { path = "../lui-core" }
lui-cascade = { path = "../lui-cascade" }
lui-layout = { path = "../lui-layout" }
lui-glyph = { path = "../lui-glyph" }
